apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: nginx
  namespace: dev
spec:
  host: nginx-dev.apps.dub.idiro.com
  port:
    targetPort: 8080 
  to:
    kind: Service
    name: nginx
